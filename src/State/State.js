import {getDatasetMinMax} from '../utils';

export default class State {
  constructor() {
    // this.firstIndex = null;
    // this.lastIndex = null;
    // this.startIndex = null;
    // this.endIndex = Math.ceil(lastIndex * range[1]);
    //
    // updateAxisY(this.axisY, getDatasetMinMax(dataset, this.startIndex, this.endIndex));
    //
    // const { axisY } = this;
    //
    // this.stepX = realWidth / lastIndex;
    // this.stepY = vp.height / (axisY.max - axisY.min);
    // this.offsetX = -realWidth * range[0];
    // this.offsetY = axisY.min * this.stepY;
  }
}
